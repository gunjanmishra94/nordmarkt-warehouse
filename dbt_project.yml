name: "nordmarkt"
version: "1.0.0"
config-version: 2
profile: "nordmarkt"

model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  nordmarkt:
    staging:
      +materialized: view
    marts:
      +materialized: table
