# Streamlit Community Cloud installs from this file, not the root pyproject.toml/uv.lock
# (its resolver assumes Poetry-style pyproject.toml, not uv's). Keep these in step with
# the dependencies in the root pyproject.toml.
streamlit>=1.38
dbt-core>=1.8
dbt-duckdb>=1.8
dlt[duckdb]>=1.4
pydantic>=2.7
faker>=25.0
pandas>=2.2
pyarrow>=16.0
requests>=2.31
duckdb>=1.0
